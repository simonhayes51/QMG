"use client";import {useEffect,useState} from 'react';export default function ThemeToggle(){const [theme,setTheme]=useState('dark');useEffect(()=>{const stored=localStorage.getItem('theme');const prefers=window.matchMedia('(prefers-color-scheme: light)').matches?'light':'dark';const t=stored||prefers;setTheme(t);document.documentElement.setAttribute('data-theme',t)},[]);const toggle=()=>{const t=theme==='dark'?'light':'dark';setTheme(t);document.documentElement.setAttribute('data-theme',t);localStorage.setItem('theme',t)};return <button onClick={toggle} className='btn btn-ghost' aria-label='Toggle theme'>{theme==='dark'?'🌙':'☀️'}</button>;}