export const metadata={title:'Venues — The Quizmaster General'};export default function Page(){const logos=Array.from({length:10},(_,i)=>`/assets/v${i+1}.jpg`);return(<section className='container-pad py-12'><h1 className='text-3xl font-bold'>Venues</h1><p className='opacity-70 mt-2'>A sample of pubs, bars & spaces we light up.</p><div className='mt-6 grid grid-cols-2 sm:grid-cols-3 md:grid-cols-5 gap-4'>{logos.map((src,i)=>(<div key={i} className='glass p-3 flex items-center justify-center h-28'><img src={src} alt={`Venue ${i+1}`} className='object-contain max-h-16 w-auto'/></div>))}</div></section>);}